import { LoadingOutlined } from '@ant-design/icons'
import React from 'react'

// Иконка загрузки из antd
export const antIcon = (
	<LoadingOutlined
		style={{
			fontSize: 40,
			color: '#fff',
		}}
		spin
	/>
)
